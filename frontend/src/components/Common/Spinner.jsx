import React from 'react';

const Spinner = () => {
  return (
    <div className="spinner">
      <div className="loading">Carregando...</div>
      {/* Aqui você pode adicionar uma animação CSS ou SVG para o spinner */}
    </div>
  );
};

export default Spinner;
